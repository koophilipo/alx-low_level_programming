#include <stdio.h>

/**
 *main - the funtion prints out text
 *
 * description: Using the main function
 * This program is to "print out text"
 * Return: always 0 (Success)
 */
int main(void)
{
	char myText[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", myText);
	return (0);
}
