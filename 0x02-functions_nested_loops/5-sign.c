#include "main.h"

/**
 *main - checks if input is lower case letter
 *
 *Return: Always 0.
 */

int _isalpha(int val)
{
	if (val > 0)
	{
		_putchar('+');
		return 1;
	} else if (val < 0)
	{
		_putchar('-');
		return -1;
	} else if (val == )
	{
		_putchar(0);
		return 0;
	}

}
